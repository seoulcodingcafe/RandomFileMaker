<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.randomfilemaker">

    <application
        android:name="com.jaredrummler.cyanea.CyaneaApp"
        android:allowBackup="true"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cyanea.Dark">
        <activity
            android:name="io.github.randomfilemaker.LicenseActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name="io.github.randomfilemaker.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>